# プロジェクトの目的
- 傾向スコアを用いた因果推論を、lalondeデータセットで実施する
- 最終的には、記事骨子をもとに、記事を./article/output.mdに作成する
  - 文章は常体で

# 進め方
- まず、dev/spec.mdに、分析の手順を作成する
- []を配置して、進捗が見ればわかるようにする
- ステップを進めるごとにspec.mdを更新する
- 対話的にspec.mdを作成する

# 環境
- uvを使う（pyproject.toml, uv.lockは追加済）

# 以下は記事骨子

# 記事のゴール

- 実務で傾向スコアを用いた分析を実施する際の注意点を理解する
- 傾向スコアを使うのに適した場面を理解する

# データ

- Lalondeデータセット
    - NSWグループと、CPSグループのデータのみえられている状況（実験のコントロール群のデータは手に入っていない想定）

# 手法

- 概要
    - 各サンプルが「介入を受ける確率」を傾向スコアと呼び、推定する
    - 傾向スコアをもとにした3種類の代表的な集計方法
        - マッチング：介入を受ける確率が近いペアを作成し、差分を効果とする
            - 介入群に対してマッチングした場合はATT、対照群に対してマッチングした場合は（呼び方はわからないが）対照群における効果が算出されっる
        - 層別解析：傾向スコアによっていくつかのセグメントに分けて、結果変数の平均の差を算出する
        - IPW：介入を受ける確率の分布が等しくなるように、処置群／対照群それぞれ重みつき平均をとる
            - ATEが得られる
            - 介入グループと非介入グループの傾向スコアのさが大きい場合は通用しにくい（傾向スコアが非常に小さくなる場合に、そのサンプルの重みが非常に大きくなるため）
- 分析の前提
    - バランシングの確認
        - 傾向スコアを使うことによって、標準化平均差が小さくなっていることを確認する
        - →図示で確認、0.1以下が目安
    - 共通サポート仮定（正値性）
        - 介入を受ける確率が0<x<1になっている
        - →図示で確認

# 結果

- 傾向スコアの推定
    - ロジスティック回帰モデルにより傾向スコアを推定する
- 分析の前提の確認
    - バランシングできているか：標準化平均差が代替0.1以内に収まっているか
    - 共通サポート仮定が成り立ってるか：bin=0.05で分布を書いたときに、ほとんどの領域で、介入群・処置群のサンプル数が0ではないか
- 効果の推定
    - マッチング：介入群にたいするマッチング
    - 層別解析：傾向スコアについて、0.25ごとに区切って層別に平均を集計することによる効果の推定
    - IPW
    - 結果は下記の表にまとめる

|method|effect|

|—-|—-|

|matching|xx|

…

# 感想

- 標準化平均差や正値性の確認ができ、結果の妥当性をたしかめやすい
    - 回帰分析でも、感度分析によってOVBバイアスの評価をする方法はあったりするが、納得感が違う気がする

# 参考文献
- 効果検証入門
- 因果推論 基礎から機械学習・時系列解析・因果探索を用いた意思決定のアプローチ

### 変数の定義

- **treated**: 処置変数（1 = 職業訓練プログラムに参加、0 = 非参加）
- **age**: 年齢（歳）
- **educ**: 教育年数（年）
- **black**: 人種ダミー（1 = 黒人、0 = その他）
- **hispan**: ヒスパニック系ダミー（1 = ヒスパニック、0 = その他）
- **married**: 結婚状況（1 = 既婚、0 = 未婚）
- **nodegree**: 学位なしダミー（1 = 高卒資格なし、0 = あり）
- **re74**: 1974年の実質所得（ドル）
- **re75**: 1975年の実質所得（ドル）
- **re78**: 1978年の実質所得（ドル）- アウトカム変数
- **early_ra**: Smith and Todd (2005) の早期ランダム割り当てサンプルへの包含を示す指標変数（実験サンプルに含まれない場合は欠損値）
- **sample**: サンプルの種類（1 = NSW実験サンプル、2 = CPS比較グループ、3 = PSID比較グループ）
- **experimental**: 実験サンプルに含まれるかどうか（1 = 実験サンプル、0 = それ以外）

### サンプルグループの説明

**1. NSW実験サンプル (sample=1)**
- National Supported Work (NSW) プログラムのランダム化比較試験（RCT）データ
- 処置群と対照群がランダムに割り当てられているため、因果効果の推定が容易
- 真の因果効果のベンチマークとして使用される

**2. CPS比較グループ (sample=2)**
- Current Population Survey（米国の雇用統計調査）から抽出された非実験的な対照群
- NSW処置群と比較するために構築された観察データ
- ランダム割り当てではないため、選択バイアスが存在する可能性

**3. PSID比較グループ (sample=3)**
- Panel Study of Income Dynamics（所得動態パネル調査）から抽出された非実験的な対照群
- CPSと同様に、観察データとしてNSW処置群との比較に使用
- より長期的な所得履歴データを含む

LaLonde (1986) とその後の研究では、実験データ（NSW）で得られた因果効果と、観察データ（CPSやPSID）を用いた非実験的手法で得られた推定値を比較することで、各種因果推論手法の妥当性を検証しています。